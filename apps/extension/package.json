{
  "name": "what-is-this-extension",
  "version": "0.3.0",
  "private": true,
  "type": "module",
  "scripts": {
    "check": "node --check service-worker.js && node --check sidepanel.js && node --check guide-adapter.js && node --check session-store.js && node --check capture-source.js && node --check extension-policy.js && node --check window-operation-registry.js",
    "package:store": "node scripts/package-store.mjs",
    "render:store": "node scripts/render-store-assets.mjs",
    "test": "node --test tests/*.test.mjs",
    "validate:store-assets": "node scripts/validate-store-assets.mjs",
    "verify": "npm run check && npm test && npm run validate:store-assets"
  }
}
