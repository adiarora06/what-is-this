{
  "name": "what-is-this-extension",
  "version": "0.4.0",
  "private": true,
  "type": "module",
  "scripts": {
    "check": "node --check service-worker.js && node --check sidepanel.js && node --check guide-adapter.js && node --check session-store.js && node --check capture-source.js && node --check extension-policy.js && node --check operation-registry.js",
    "package:store": "node scripts/package-store.mjs",
    "render:store": "node scripts/render-store-assets.mjs",
    "test:ui": "node scripts/render-store-assets.mjs --verify-only",
    "test": "node --test tests/*.test.mjs",
    "validate:store-assets": "node scripts/validate-store-assets.mjs",
    "verify": "npm run check && npm test && npm run test:ui && npm run validate:store-assets"
  }
}
